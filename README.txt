Para executar o agente, basta instalar o Agent.apk em um dispositivo android.
Para executar o gerente, basta executar o Android_SNMP_Manager.jar com "java -jar Android_SNMP_Manager.jar". A pasta lib contém as bibliotecas utilizadas, e é necessária à execução do programa
